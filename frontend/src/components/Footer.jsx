import React from 'react';

function Footer() {
  return (
    <footer>
      {/* Dein Footer-Inhalt */}
    </footer>
  );
}

export default Footer; // Wichtig: Der Export muss hier sein, damit die Komponente in anderen Dateien verwendet werden kann